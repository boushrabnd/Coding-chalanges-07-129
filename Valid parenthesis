class Solution(object):
    def isValid(self, s):
        if s.find("(")== s.find(")") or s.find("{")==s.find("}") or s.find("{")==s.find("}"):
            return True
        else: 
            return False
